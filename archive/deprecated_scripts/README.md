# Deprecated Scripts

These launchers were superseded by the ordered `scripts/00-07` workflow. They
are retained only to document earlier command names and are not supported
entry points.

Use the active scripts in the repository-level `scripts/` directory.
